
# Overview

A very basic OAuth 2.0 client library for PHP. This library is intended to be used in a PHP application that needs to authenticate with an OAuth 2.0 server with either Client Credentials or Refresh Tokens.

